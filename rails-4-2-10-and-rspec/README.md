# Shoulda Matchers test app (Rails 4.2.10 + RSpec)

Quick steps:

* Generate a model (`rake g model`)
* Update the migration as you see fit
* Update the tests for the model as you see fit
* Run the tests (`rspec`)
